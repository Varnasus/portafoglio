// Placeholder component — will be swapped to use react-split-flap-display
// when the npm package is ready.
export function Headline() {
  return (
    <h1 className="text-5xl font-extrabold tracking-tight sm:text-7xl leading-[1.05] mb-6">
      I build AI systems that ship to production.
    </h1>
  )
}
